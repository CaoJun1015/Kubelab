[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "kubelab"
version = "0.1.0a0"
description = "Local Kubernetes operations practice platform"
requires-python = ">=3.11,<3.12"
dependencies = [
    "kubernetes>=31,<40",
    "pydantic>=2.10,<3",
    "pyyaml>=6,<7",
    "tomli-w>=1.2,<2",
    "typer>=0.12,<1",
]

[project.scripts]
kubelab = "kubelab.cli:app"

[dependency-groups]
dev = [
    "mypy>=1.15,<2",
    "pytest>=8,<10",
    "pytest-cov>=6,<8",
    "ruff>=0.12,<1",
    "types-pyyaml>=6,<7",
]

[tool.uv]
package = true

[tool.hatch.build.targets.wheel]
packages = ["src/kubelab"]

[tool.pytest.ini_options]
addopts = [
    "--strict-config",
    "--strict-markers",
    "-ra",
    "--cov=kubelab",
    "--cov-branch",
    "--cov-report=term-missing",
    "--cov-fail-under=90",
]
testpaths = ["tests"]

[tool.coverage.run]
branch = true
source = ["kubelab"]

[tool.coverage.report]
show_missing = true
skip_covered = false

[tool.ruff]
target-version = "py311"
line-length = 100
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"

[tool.mypy]
python_version = "3.11"
strict = true
files = ["src"]
pretty = true
show_error_codes = true
warn_unused_configs = true

[[tool.mypy.overrides]]
module = ["kubernetes.*"]
ignore_missing_imports = true
