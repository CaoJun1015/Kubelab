apiVersion: v1
kind: Service
metadata: {name: web-headless, namespace: kubelab-statefulset-headless}
spec:
  clusterIP: None
  selector: {app: stateful-headless-lab}
  ports: [{name: http, port: 80, targetPort: http}]
