apiVersion: kubelab.io/v1alpha1
kind: LabAuthoringContract
scenarioType: baseline
states:
  faulted:
    observations:
      pvc-remains-pending:
        type: pvc_status
        phase: Pending
      pvc-bound:
        type: pvc_status
        phase: Pending
      storage-consumer-ready:
        type: pod_status
        pods:
        - name: consumer-abc
          phase: Pending
          ready: false
          restartCount: 0
          containers:
          - name: consumer
            image: busybox:1.36.1
            ready: false
            restartCount: 0
            state: running
  repaired:
    observations:
      pvc-remains-pending:
        type: pvc_status
        phase: Bound
      pvc-bound:
        type: pvc_status
        phase: Bound
      storage-consumer-ready:
        type: pod_status
        pods:
        - name: consumer-abc
          phase: Running
          ready: true
          restartCount: 0
          containers:
          - name: consumer
            image: busybox:1.36.1
            ready: true
            restartCount: 0
            state: running
  reset: faulted
repairs:
  full:
    manifest: solutions/fix.yaml
    allowedChanges:
    - resource:
        apiVersion: v1
        kind: PersistentVolumeClaim
        name: data
      operation: recreate
      paths:
      - /spec/storageClassName
