apiVersion: v1
kind: PersistentVolumeClaim
metadata: {name: data, namespace: kubelab-pvc-pending}
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: standard
  resources:
    requests: {storage: 128Mi}
